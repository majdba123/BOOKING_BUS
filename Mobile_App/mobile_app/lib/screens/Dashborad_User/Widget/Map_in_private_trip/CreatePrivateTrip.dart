// Legacy prototype retained for reference only.
//
// If this flow is restored, obtain the Maps key from the centralized build
// configuration (`AppConfig.requireGoogleMapsApiKey()`) rather than embedding a
// key in source code. Production Google Maps keys must also be restricted in
// Google Cloud by API and by the relevant application identifier/origin.
